# Configs

AI-Toolkit (and later Musubi Tuner) configs that actually completed a training run, with inline comments explaining each setting.

Filename convention: `<trainer>-<model>-<descriptor>.yaml` — e.g., `ai-toolkit-flux-first-lora.yaml`.
